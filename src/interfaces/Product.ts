export interface IProduct {
    id?: number
    name: string
    price: number,
    img: string
}
export interface User {
    id?: number
    email: string
    password: string,
    Confirmpassword: string,
}
