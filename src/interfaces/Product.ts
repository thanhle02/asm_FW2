export interface IProduct {
    id?: number
    name: string
    price: number,
    img: string
}
export interface User {
    id?: number,
    name: string,
    email: string
    password: string,
    confirmPassword: string,
}
